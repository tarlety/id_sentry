{
  "name": "id-sentry",
  "productName": "ElectronIDSentry",
  "version": "1.0.0",
  "description": "Simple Application to Record ID",
  "repository": "",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "package:linux": "electron-packager . --overwrite --platform=linux --arch=x64 --out=out",
    "package:win": "electron-packager . --overwrite --platform=win32 --arch=ia32 --out=out --icon=res/icon.png"
  },
  "license": "ISC",
  "devDependencies": {
    "electron": "^3.0.2",
    "electron-packager": "^14.2.1"
  },
  "dependencies": {
    "crypto": "^1.0.1",
    "electron-store": "^5.1.1",
    "jquery": "^3.4.1"
  }
}
